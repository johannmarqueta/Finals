services:
  - type: web
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: sh -c 'python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn myproject.wsgi:application --bind 0.0.0.0:$PORT'